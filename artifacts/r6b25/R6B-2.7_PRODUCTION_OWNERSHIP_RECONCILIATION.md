# R6B-2.7 — WINDOW 1: PRODUCTION OWNERSHIP RECONCILIATION (READ-ONLY)

**Repository:** /home/shunya-deploy/shunya_os @ 3389e47e365054c24e42147a52f5acc020773ee0 (no commit/push/deploy)
**Production inspected:** PostgreSQL 16.15, `localhost:5432`, database `shunya_os`
**Access mode:** strictly READ-ONLY. Session opened with `set_session(readonly=True)` and
`default_transaction_read_only` / `transaction_read_only` both verified `on` at query time.
No INSERT / UPDATE / DELETE / ALTER / migration / seed / repair was executed. Credential taken from
the application's own `.env`; never printed.
**Raw inventory:** `artifacts/r6b25/r6b27_production_inventory.json`
**Inspector:** `scripts/r6b27_production_reconciliation.py`

---

## 1. Production counts

| Metric | Value |
|---|---|
| organizations | 159 |
| org_members (total / active) | 167 / 167 |
| identities with active membership | 161 |
| **identities with MULTIPLE active organizations** | **1** |
| sh_objects | 483 |
| **sh_workspaces (total)** | **4** |
| sh_workspaces with `organization_id IS NULL` | 1 |
| sh_workspaces with status `active` | 3 |
| sh_workspaces containing canonical objects | 3 |
| user_workspaces | 1 |
| user_workspaces with `organization_id IS NULL` | 1 |
| user_workspace_memberships (total / active) | 1 / 1 |
| founder_spaces / founder_objects | 3 / 1 |
| `workspaces` (third system, `tenant_id`-keyed) | table exists |

## 2. `sh_workspaces` — complete production contents (4 rows, all enumerated)

| id | organization_id | status | created_by | workspace_type | classification |
|---|---|---|---|---|---|
| `spc_default` | **NULL** | **NULL** | `system` | personal | **LEGACY / NON-CANONICAL** |
| `spc_custom` | 1 | active | `system` | custom | AUTHORITATIVE (org known) **but ORPHANED** (no identity authorization) |
| `spc_personal` | 1 | active | `system` | personal | AUTHORITATIVE (org known) **but ORPHANED** |
| `spc_business` | 7 | active | `system` | business | AUTHORITATIVE (org known) **but ORPHANED** |

Two findings of immediate consequence:

1. **`spc_default` still exists in production** with `organization_id IS NULL` and `status IS NULL`.
   Commit 3389e47 ("spc_default removal") removed this identifier from *code*; the production **row**
   remains. It is a legacy non-canonical row: no organization, no status, no `created_by` identity
   (only the literal `"system"`). It must not become a hidden universal workspace, and it cannot be
   authoritatively attached to an organization from persisted data.

2. **Organization 1 owns TWO `sh_workspaces` (`spc_custom` and `spc_personal`).** This is precisely
   the ambiguous case: any `Workspace.query.filter_by(organization_id=1).order_by(id).first()` would
   have selected `spc_custom` — an arbitrary, unauthorized choice between two legitimate rows. The
   arbitrary-selection defect is therefore **live in production data**, not hypothetical.

   `organization_id = 1` also calls for scrutiny: org 1 is the "Test Org" referenced in
   `g1_1_r1_fix_org_chain` (legacy remap 90 → 1). Whether production objects should reside in org 1
   at all is a data question for you, not something I will silently change.

## 3. `sh_objects` distribution

483 canonical objects exist across 3 workspaces. Every one of those objects references an
`sh_workspaces` row whose only ownership data is `organization_id` (1, 1, 7) and
`created_by = "system"`. **The numeric/string identity that created them is not recorded on the
workspace**, so no identity can be authoritatively derived as authorized.

## 4. `user_workspaces` (1 row)

| workspace_id | owner_identity_id | organization_id | status |
|---|---|---|---|
| `ws_55a4818e1685` | `sid_a3cd655b1e6f4b0f9c1113ba7ec26d41` | **NULL** | active |

## 5. `user_workspace_memberships` (1 row)

| identity_id | workspace_id (int FK) | role | is_active |
|---|---|---|---|
| `sid_a3cd655b1e6f4b0f9c1113ba7ec26d41` | 4 | owner | true |

Orphan check: **0** memberships pointing at a missing `user_workspaces` row.

## 6. Mapping classification (`user_workspaces` → `sh_workspaces`)

The single `user_workspaces` row is `ws_55a4818e1685`, owned by `sid_a3cd655b…`, with
`organization_id = NULL`. The four `sh_workspaces` rows are `spc_default`, `spc_custom`,
`spc_personal`, `spc_business`.

| Candidate pairing | Authoritative basis? | Classification |
|---|---|---|
| `ws_55a4818e1685` → `spc_business` | none — different id namespaces (`ws_…` vs `spc_…`), no FK, no mapping column, no code writing one from the other, and the source row's `organization_id` is NULL while the target's is 7 | **UNRESOLVED** (a name/form-similarity mapping would be exactly what you prohibited) |
| `ws_55a4818e1685` → `spc_personal` | none (same reasons; "personal" is a type, not evidence of identity) | **UNRESOLVED** |
| `ws_55a4818e1685` → `spc_custom` | none | **UNRESOLVED** |
| `ws_55a4818e1685` → `spc_default` | none, and the target is legacy/non-canonical and organization-less | **UNRESOLVED** |
| `spc_business` → org 7 | `sh_workspaces.organization_id` is persisted and set | **AUTHORITATIVE** for organization; **ORPHANED** for identity authorization |
| `spc_custom` → org 1 | same | **AUTHORITATIVE** for organization; **ORPHANED** |
| `spc_personal` → org 1 | same | **AUTHORITATIVE** for organization; **ORPHANED** |
| `spc_default` → any org | no | **LEGACY / NON-CANONICAL** |

**There are ZERO authoritative identity→workspace authorizations for any canonical `sh_workspaces`
row in production.** `created_by = "system"` on all four rows is a sentinel, not an identity.

Consequence for Window 8: existing production `sh_workspaces` rows **cannot** be granted memberships
from persisted data. Per your ruling ("if there isn't one, mark it unresolved rather than inventing
ownership"), they are recorded **UNRESOLVED** — no membership is fabricated.

## 7. Multi-organization identities in production

Exactly **one** identity holds multiple active organizations:

| identity_id | active organizations |
|---|---|
| `neg_test_user` | **7** |

The identifier itself (`neg_test_user`) indicates a **test artifact that exists in production data**.
It is not deleted, modified, or interpreted further — flagged for your attention as a data-hygiene
item, and as a live example of why `OrgMember…filter_by(identity_id=…).first()` cannot be an
authorization decision: for this identity, `.first()` would silently pick one of seven organizations.

## 8. Implications carried into the implementation windows

1. **Organizations are plural in production** → the canonical resolver must require an explicit
   organization selection whenever more than one active membership exists, and fail closed otherwise
   (your ruling 2).
2. **Org 1 has two canonical workspaces with objects** → automatic single-workspace resolution must
   not fire for org 1; both must remain explicit.
3. **No identity authorization exists for any canonical workspace** → after the new model ships,
   production object access would fail closed for every current user until memberships are
   provisioned. This is a **deployment-shaping fact** requiring your decision; it must not be
   discovered in production.
4. **`spc_default` is a legacy row with NULL organization** → it must not be treated as a
   workspace, and it is a candidate for explicit retirement (a human decision; I will not delete it).
5. **`neg_test_user` (7 orgs) in production** → human action required: confirm whether it is
   intentional.

## 9. Window 1 status

**COMPLETE** — production inspected read-only; full inventory captured; every candidate mapping
classified; nothing repaired, merged, deleted, or inferred.

Blockers surfaced for human decision (not silently resolved):

- **B1** — No authoritative identity→workspace authorization exists for the 4 production
  `sh_workspaces` rows. Provisioning real memberships for live users requires the founder to state
  who is authorized, or an explicit decision to let all current users fail closed until re-provisioned.
- **B2** — `spc_default` legacy row (NULL organization, NULL status) present in production.
- **B3** — Organization 1 holds two canonical workspaces and carries the legacy "Test Org" remap;
  confirm intended ownership.
- **B4** — `neg_test_user` with 7 active organizations present in production data.
