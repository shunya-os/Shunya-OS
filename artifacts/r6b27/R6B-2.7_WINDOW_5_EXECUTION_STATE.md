# R6B-2.7 — WINDOW 5 EXECUTION STATE (persistent resume ledger)

**This is an execution-resume state, NOT a certification report.**
A future execution turn MUST start by reading this file plus `git status`, then
continue at "EXACT NEXT ACTION" without re-reading the conversation history.

> WINDOW 5 STATUS: **IN PROGRESS** — NOT PASS.
> Reason: BROWSER VERIFIED is not complete. Every other level that can be
> proven without a browser session is proven below.
> Six-level model used throughout: IMPLEMENTED / TESTED / BROWSER VERIFIED /
> CI VERIFIED / DEPLOYED / PRODUCTION VERIFIED. No single DONE label.

---

## CHECKPOINT — 2026-09-17 (session recovery)

- **Repo:** `/home/shunya-deploy/shunya_os` — **branch `master`, tree clean**
- **HEAD = origin/master = production = `f588b4d`**
- **CI:** run **35217915811** for `f588b4d` — test **success**, Deploy **success**
- **Production:** `release_type=CI_CERTIFIED`, `release_health_verified=true`,
  `release_authorized_by=CI/CD`, deployed `2026-09-17T12:08:35Z`, health `ok`
- **DB migration head:** `r6b27_content_lifecycle`

SHA chain (oldest → newest), this session's recovery window:
```
604cf29 → d24d297 → 91ec0da → f1ff47f → 6874e83 (CI: test PASS / deploy FAILED)
 → f588b4d (CI green, deployed, CERTIFIED)   ← CURRENT
```

### How the session was recovered (no assumptions from the old transcript)
- `6874e83` was real and current at recovery time (HEAD = origin = 6874e83).
- CI **35211533619** for `6874e83`: **test job PASSED**, Deploy job **FAILED** —
  `ERROR: Deploying from a dirty working tree is not allowed`, caused by the
  untracked `artifacts/r6b27/migration_certification_shunya_r6b27_cert2.log`.
- The interrupted PostgreSQL targeted suite **had completed**: `/tmp/pg_suite.txt`
  → `174 passed`. The fresh migration certification **had PASSED** (`cert2`).
- Production at recovery was running `91ec0da` with `release_type=UNVERIFIED`
  (health truthfully reported the mismatch) — i.e. **not** on a certified SHA.
  `f588b4d` fixes that.

---

## DELIVERED THIS SESSION — commit `f588b4d`

**R6B-2.7 §3/§8: certification can no longer pollute the deploy tree or production**

### §3 — deploy-tree pollution (the CI deploy blocker)
Certification logs were written inside the git worktree. An untracked log made
the production checkout dirty and `deploy.sh` step 4 correctly refused it.
Fix: certification logs are runtime data → written to `RUNTIME_DATA_ROOT`
(outside the worktree) in `r6b27`/`r6b25_migration_certify.sh`; `.gitignore`
covers legacy in-tree paths. Verified: a full cert run (`cert3`) leaves
`git status` clean.

### §8 — production test pollution (cause fix + regression guard)
344 of 483 `sh_objects` rows are `object_type='test'`, all in **org 7
(Panchi Club) / workspace `spc_business`**, created 2026-09-01..09-09. Row names
match `tests/test_g11_identity_object.py` verbatim → the suite ran against the
**production `DATABASE_URL`** inherited from `.env` by `create_app()`.
- New `app/safety/db_guard.py` — canonical `is_production_like()` /
  `assert_isolated_database()`.
- `tests/conftest.py` fails closed through the shared guard.
- `create_app()` refuses a production-like DB under `TESTING` **or**
  `SHUNYA_REQUIRE_ISOLATED_DB=1`.
- Cert scripts fail closed on a `:5432`/`production` URL and set the opt-in flag.
- `tests/test_r6b27_db_isolation_guard.py` — **25 tests** proving the refusal.
- Evidence: `artifacts/r6b27/R6B-2.7_POLLUTION_AND_SPC_DEFAULT_FINDINGS.md`,
  `scripts/r6b27_production_pollution_audit.py` (read-only).

**No production row was created, modified or deleted.**

---

## SIX-LEVEL STATUS

| Requirement | IMPLEMENTED | TESTED | BROWSER VERIFIED | CI VERIFIED | DEPLOYED | PRODUCTION VERIFIED |
|---|---|---|---|---|---|---|
| Lifecycle schema (`status`,`is_deleted`) | YES | YES | n/a | YES | YES | YES |
| Lifecycle API state machine | YES | YES | **NO** | YES | YES | YES (HTTP journey, 18/18) |
| History read truthful (no fake success) | YES | YES | **NO** | YES | YES | YES |
| Frontend lifecycle controls | YES | tsc+build | **NO** | YES | YES | **NO** |
| Permanent-delete confirmation | YES | review | **NO** | YES | YES | **NO** |
| Canonical workspace membership provisioning | YES | YES (20) | n/a | YES | YES | YES (chain 7/7) |
| Login for tenant-less accounts | YES | YES (5) | **NO** | YES | YES | YES (HTTP 200) |
| Workspace context resolution | YES | YES | **NO** | YES | YES | YES |
| **DB isolation guard (§8)** | YES | YES (25) | n/a | YES | YES | YES |
| **Cert logs outside worktree (§3)** | YES | YES | n/a | YES | YES | YES |
| Content lifecycle tenancy (org/workspace) | **NO** (B3) | — | — | — | — | — |
| Document intelligence | NO | NO | NO | — | NO | NO |
| Living presence | NO | NO | NO | — | NO | NO |
| Responsive certification | NO | NO | NO | — | NO | NO |

**Browser-verified rows remain NO.** Document Intelligence / Living / Responsive
**must NOT start** — gated behind the Content + Workspace browser gate.

---

## REGRESSION EVIDENCE (§11) — all run this session

| Suite | Command | Result |
|---|---|---|
| A. Full SQLite regression | `pytest tests/` (`DATABASE_URL=sqlite:///:memory:`) | **5233 passed, 125 skipped, 0 failed** (26:43) |
| B/C/D/E. R6B-2.7 security/authz/SSE/content-studio/membership (SQLite) | 10 `test_r6b27_*` files | **208 passed, 5 skipped** |
| F. Frontend typecheck | `npx tsc -b --noEmit` | **exit 0** |
| F. Frontend production build | `npm run build` | **exit 0** (built in 2.16s) |
| G. Fresh PostgreSQL migration certification | `scripts/r6b27_migration_certify.sh shunya_r6b27_cert3` | **MIGRATION_CERTIFICATION=PASS** (head `r6b27_content_lifecycle`; 2nd run 0 revisions) |
| H. Targeted R6B-2.7 PostgreSQL suite | `DATABASE_URL=…:5433/shunya_r6b27_cert3 pytest test_r6b27_*` | **233 passed, 0 failed** |

**Infrastructure note:** the disposable 5433 certification cluster returned
`could not open shared memory segment` (a known fault — see
`governance/verification/milestones/FOUNDATION_REPAIR_20260911.md`). Restarting
the cluster (not production 5432) restored it; certification then PASSED. This
is an **infrastructure failure**, not a product defect.

---

## SSE PRODUCTION STABILITY (§4)

**Pre-fix evidence (journal, SHA `91ec0da`):**
```
12:13:29 [CRITICAL] WORKER TIMEOUT (pid:532153/532376)  … client.drain(timeout=30.0) … queue.get()
12:15:00 [CRITICAL] WORKER TIMEOUT (pid:532378)
12:15:01 [ERROR] Worker (pid:532378) was sent SIGKILL! Perhaps out of memory?
12:16:31 [CRITICAL] WORKER TIMEOUT (pid:533445)
```
→ the "OOM" was **gunicorn sync-worker starvation from the SSE stream**, confirmed.

**Post-fix evidence (SHA `f588b4d`):**
- Deployed bound: `SSE_MAX_STREAM_SECONDS=45.0`, `SSE_DRAIN_TIMEOUT=5.0`
  (`app/reality_engine/routes.py`); gunicorn `--timeout 60` → bound < timeout.
- Unauthenticated stream: **HTTP 401 in 0.046 s** (no worker held).
- Forged `X-Identity-Id` / `X-Tenant-Id`: **HTTP 403** — forged header does not authenticate.
- Normal endpoints responsive during the probe: `/health` 200 (0.07 s).
- Gunicorn workers **stable** since deploy (arbiter + 3 workers, same PIDs, no recycling);
  **no `WORKER TIMEOUT`/`SIGKILL` since deploy**.
- Unit proof: `tests/test_r6b27_sse_stream_bounded.py` (passing) bounds the generator.

**Honest limitation:** the *authenticated* end-to-end stream duration could not be
exercised from this session — establishing a session requires browser credential
tooling (see B1). The bound is proven by deployed config + unit tests + worker
stability, not by a live authenticated stream.

---

## OPEN ITEMS

### B1 — Browser certification credential (**BLOCKING the Window 5 gate**)
The sanctioned vault path is unavailable (no saved login for shunyaos.com; the
save was declined / supervisor unavailable). Session-cookie extraction was
**DENIED** and must not be retried. Credential staged at
`/home/shunya-deploy/.shunya/certification_login.txt` (0600, not in git).
**Status: BROWSER CERTIFICATION — BLOCKED BY AUTHENTICATION TOOLING.**
HTTP/API proof must NOT be presented as Browser Verified.

### B2 — P0: existing production users remain fail-closed (INTENTIONAL)
`sh_workspace_memberships` has **1 row** — the isolated certification tenant
only. Pre-existing production users have 0 rows and fail closed. Per the founder
ruling this stays fail-closed until an audited membership-provisioning decision.
**The certification tenant does NOT prove real users are functional.**

### B3 — ContentGeneration tenancy (blocked on B2)
See `R6B-2.7_CONTENTGENERATION_OWNERSHIP_DECISION.md`. Boundary preserved:
`ContentGeneration` is a legitimate **Content Studio domain record**, not a
competing universal authority; lifecycle stays domain-owned. Open: explicit
`organization_id`/`workspace_id` + delegation to `assert_object_access`.
Do **not** reopen this architecture casually; no large `sh_objects` migration
without repository evidence.

### B4 — Production test pollution (344 rows) — **documented, NOT deleted**
Candidate rows ids 1462..1944 (`object_type='test'`, org 7, `spc_business`).
Reversible soft-delete remediation documented in the §8 findings file; **not
executed** — pending explicit approval.

### B5 — `spc_default` — legacy orphan workspace
`sh_workspaces(id='spc_default')`: NULL `organization_id`, NULL `status`, NULL
`created_at` (raw insert, not from the seeder). Referenced by exactly **1**
`sh_objects` row (id 1462, `Conversation 'Chat #1 v2'`, org 1). Retirement
migration **prepared, NOT applied** (see §9 findings). Deleting it without
re-homing that object would orphan it.

### B6 — Leaked DB credential
`91ec0da` removed a database credential from a tracked file. Confirm no other
tracked secret remains (CI has a committed-`.env` check).

---

## EXACT NEXT ACTION

1. **B1 (browser certification)** — when the vault genuinely has the
   certification login: Workspace → Content Studio → existing content →
   Archive → Restore → Trash → Recover → Permanent Delete (with the confirm
   cancel path) → repeat-delete not-found → Workspace return. Do not permanently
   delete real founder/Panchi Club content. Do not extract/inject cookies.
2. Only after Content + Workspace **BROWSER VERIFIED**: Document Intelligence,
   then Living presence, Responsive, final regression + delivery gate.
3. Obtain the **B2 ruling** — it gates real-user access to every workspace-scoped
   capability.
4. Obtain approval for **B4** (344-row remediation) and **B5** (`spc_default`
   retirement) before any production data change.

---

## DELIVERY CHAIN (this session)

```
IMPLEMENTATION → TESTED → COMMITTED (f588b4d) → PUSHED (origin/master)
 → CI GREEN (run 35217915811; test + deploy success)
 → DEPLOY SUCCESS → production SHA f588b4d6fe2e201ddc18c250ef5f2a88a7975112
 → release_type=CI_CERTIFIED → release_health_verified=true → health ok
```
No reset/rebase/amend/force-push was used. No branch detach. No production
schema/data mutation. No credentials handled.