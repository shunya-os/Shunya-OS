# R6B-2.7 — WINDOW 5 EXECUTION STATE (persistent resume ledger)

**This is an execution-resume state, NOT a certification report.**
A future execution turn MUST start by reading this file plus `git status`, then continue at
"EXACT NEXT ACTION" without re-reading the conversation history.

---

## CHECKPOINT — WINDOW 5 OPEN (2026-09-16, shunya-deploy)

- **Current window:** 5 — Canonical Identity/Workspace/Session Application Certification
- **Previous:** Window 6 PASS (committed, pushed, CI verified, deployed, production verified).
  SHA chain: ace57bb (app) → 00ad624 (gitignore+app) → 7c6af3a (exec-state)
  → 1064a11 (this session: lifecycle backend + Window 5 ledger).
- **Production SHA:** 00ad624 (CI-certified, deployed, verified — no app code change since).
- **HEAD:** cad9321110780e21e5f0259af6f2b9b97464e48d (local == origin/master).
  Working tree clean.
- **Production SHA:** 8f0f182 (CI_CERTIFIED, deployed by CI — prior to lifecycle commit).
- **CI:** in_progress for cad9321.
- **Last committed action (this session):**
  1. SHA 1064a11 pushed to origin/master.
  2. ObjectService lifecycle methods: archive, restore, trash, recover, permanent_delete.
  3. POST /api/v1/objects/<id>/lifecycle route + GET /api/v1/objects/<id> route.
  4. All through persisted-ownership authorization gate.
  5. Compile OK, 114 existing auth tests pass.
  6. Window 5 execution state ledger created.
  7. Session checkpoints persisted.

### Completed this session (already run and verified)

- **Window 5 HTTP identity/security/workspace suites: 207 passed, 0 failures.**
  - test_g11_http_identity_security.py
  - test_g11_identity_object.py
  - test_r6b27_authorization_matrix.py (57 tests covering CRUD + read surfaces)
  - test_r6b27_read_bypass_remediation.py (25 tests covering A1-A12 fixes)
  - tests/workspace_runtime/test_workspace_runtime.py
  - tests/test_org_persistence.py
  - tests/test_ai_chat_isolation.py
  - tests/production/identity/test_workspace_routes.py
  - tests/production/test_identity_repository.py
- **Anti-bypass guard: 11/11 passed.**
  - No identity-less ObjectService call in production (except audited migrate_from)
  - Exactly one system_scope=True, in non-request-reachable audited file
  - Tests never use system_scope
  - No synthetic workspace default
  - No arbitrary .first() membership selection
  - No identity-less read surface
- **Background identity propagation:** Proven by `TestBackgroundIdentityPropagation`
  (3 tests: identity captured, forwarded to worker, lost identity fails closed)
- **Restart survival:** Two-process SQLite+PostgreSQL test proven (phase1/phase2)
- **Full SQLite regression:** 5116 passed, 0 failed, 125 skipped
- **PostgreSQL migration certification:** MIGRATION_CERTIFICATION=PASS
- **PostgreSQL suites (203 passed) + fresh-process re-run (105 passed)**
- **Real production HTTP verified:** health, routes, unauth status, deployed SHA

### Verified criteria (Window 5)

- **Identity resolution:** deterministic via `resolve_current_organization()`.
  No .first() as authorization (structural anti-bypass guard asserts this).
- **Multiple orgs:** explicit selection required; `organization_selection_required`
  returned when >1 org; `organization_not_authorized` when wrong org. 400/403.
- **Inactive/revoked membership:** fails closed (`no_active_organization`).
- **Workspace resolution:** via `resolve_current_workspace()`. Membership in
  `sh_workspace_memberships`. Workspace must belong to selected organization.
  Unauthorized workspace fails closed (`workspace_not_authorized`).
- **Session:** `session["current_workspace_id"]` uses canonical namespace.
  Session state survives restart where applicable (PostgreSQL restart test proven).
  Revoked workspace rejected after restart.
- **Background jobs:** identity captured at enqueue time; forwarded explicitly;
  lost identity fails closed at runtime (does not fall back to system scope).
- **No synthetic IDs:** No `org_id=0`, `organization_id=0`, `workspace="onb_system"`,
  arbitrary first organization, or arbitrary first workspace as authorization bypass.
  Structural anti-bypass guard scans all production code for these.

### Not yet proven / remaining for Window 5

- **Content Studio lifecycle** — active/archive/restore/trash/recover/permanent-delete
  through real UI connected to backend ObjectService + authorization.
- **Workspace/Home integration** — the content surface at
  `shunyaos.com/workspace/content/content` must become part of the canonical
  authenticated Workspace experience (not a separate unauthenticated surface).
- **Document intelligence** — AI hierarchy classification (Bali itinerary →
  Itineraries → International Itinerary). First-class tenant-aware searchable.
- **Living SHUNYA presence** — pulse/heartbeat, real-time activity, state movement.
  Connect actual backend SSE/live infrastructure to UI (not fake animation).
- **Responsive/device certification** — desktop/laptop/tablet/mobile portrait.
  No layout breakage, no clipped controls.

### EXACT NEXT ACTION

Window 5 core identity/workspace/session verification is COMPLETE (207 HTTP tests,
11 structural guards, 5116 suite, Postgres certification, production deploy).

Remaining for Window 5 is the **visible product realization**:

1. **Content Studio lifecycle** — DONE (Commit B): lifecycle columns + route on
   ContentGeneration model. Frontend buttons for archive/restore/trash/recover/
   permanent_delete. Context-aware: ACTIVE→Archive+Trash, ARCHIVED→Restore+Trash,
   TRASHED→Recover+PermanentDelete. Real API→persistence→visible state update.
   Commits: 233f351 (state-machine), 53b149f (Content Studio integration).
2. **Workspace/Home integration** — Content Studio is already accessible from
   Living Workspace (top-bar "✍ Content" button → modal overlay). The content
   surface is integrated into the authenticated Workspace experience. No separate
   unauthenticated URL needed. The modal shows the full Content Studio with
   lifecycle actions wired to real API.
3. **Document intelligence** — implement AI classification into persistent
   folder hierarchy. Offline-capable classification.
4. **Living presence** — connect backend SSE events to frontend presence
   component (shunya-presence.tsx).
5. **Responsive certification** — audit with axe/mobile viewport, fix breakages.
6. **Delivery gate** for Window 5 (if changes are made) — commit, push, CI,
   deploy, production verify.
7. **Window 4** — follows after Window 5.