# R6B-2.7 — WINDOW 5 EXECUTION STATE (persistent resume ledger)

**This is an execution-resume state, NOT a certification report.**
A future execution turn MUST start by reading this file plus `git status`, then
continue at "EXACT NEXT ACTION" without re-reading the conversation history.

> WINDOW 5 STATUS: **IN PROGRESS** — not PASS.
> Six-level model used throughout: IMPLEMENTED / TESTED / BROWSER VERIFIED /
> CI VERIFIED / DEPLOYED / PRODUCTION VERIFIED. No single DONE label.

---

## CHECKPOINT — 2026-09-17 (later)

- **Repo:** `/home/shunya-deploy/shunya_os` — **branch `master`, tree clean**
- **HEAD = origin/master = production = `604cf29`**
- **CI:** run for 604cf29 — test **success**, deploy **success**
- **Production:** `release_type=CI_CERTIFIED`, `release_health_verified=true`,
  deployed 2026-09-17T09:41:38Z
- **DB migration head:** `r6b27_content_lifecycle`

SHA chain (oldest → newest):
```
ace57bb → 00ad624 → 7c6af3a → 1064a11 → 8f0f182 → 233f351 → cad9321
 → 53b149f → 322abe8 (CI FAILED) → f4d903f (CI OK, deployed)
 → 2c9cbfb → f4d4c3d (CI OK, deployed) → c6fbcc8 → ab85c41
 → 1f69470 → c2b2a3b → 604cf29 (CI OK, deployed)   ← CURRENT
```

---

## DELIVERED THIS SESSION

### Commit A — `f4d903f` — Content Studio lifecycle remediation
See `R6B-2.7_WINDOW_5_TRUTH_CORRECTION.md`. Production DB was missing
`status`/`is_deleted` (added by `r6b27_content_lifecycle`); the lifecycle
transition table used `setattr` lambdas so **every** action returned 400; the
history endpoint reported a **fake empty success** on storage errors; permanent
delete had no confirmation; the frontend failed silently.

### Commit C(1) — `c6fbcc8` / `ab85c41` — canonical workspace membership
`app/authz/workspace_membership.py` — the missing writer for
`sh_workspace_memberships`. Nothing in the codebase wrote that table, so every
`assert_object_access()` caller failed closed for every user. 20 tests.
Tests use the real actor path; `system_scope` only as a contradictory probe.
The anti-bypass guard now recognises `actor_identity_id` (still flags a bare
`system_scope=True`; self-test added).

### P0 login defect — `1f69470`
`POST /login` returned **HTTP 500** for any account without a tenant
(`NotNullViolation on persons.tenant_id`). Signup deliberately creates
tenant-less accounts, so every future signup was broken. Fixed by skipping the
Person projection rather than inventing a tenant. 5 tests.

### Tenanted Person / schema alignment — `c2b2a3b`
- `IdentityService.split()` created a Person with no tenant → inherited now.
- `_find_or_create_person()` created a Person with no tenant **and** passed
  `email`/`phone` kwargs that `Person` does not have (TypeError — the create
  path never worked); its lookup was **unscoped**, so it could match another
  tenant's person. All fixed; lookup is tenant-scoped.
- `Person.tenant_id` model aligned to `nullable=False` (the authoritative
  migration 0005 and the live DB). The stale `nullable=True` is the root cause
  of this whole defect class: tests built from the model accepted what
  production rejects.

### Ops — `f4d4c3d` + `604cf29` — deploy must never detach HEAD
The deploy checked out the certified SHA as a detached HEAD and stayed detached
for the whole (multi-minute) run. **Two commits were lost off-branch this
session** and recovered by cherry-pick. Now, when the certified SHA is the
branch head, the deploy checks out `master` and hard-resets, so HEAD never
leaves the branch. Verified: `604cf29` deployed and the repo is still on
`master`.

---

## SIX-LEVEL STATUS

| Requirement | IMPLEMENTED | TESTED | BROWSER VERIFIED | CI VERIFIED | DEPLOYED | PRODUCTION VERIFIED |
|---|---|---|---|---|---|---|
| Lifecycle schema | YES | YES | n/a | YES | YES | YES |
| Lifecycle API state machine | YES | YES (22) | **NO** | YES | YES | YES (18/18 HTTP journey) |
| History read truthful | YES | YES | **NO** | YES | YES | YES |
| Frontend lifecycle controls | YES | tsc+eslint+build | **NO** | YES | YES | **NO** |
| Permanent-delete confirmation | YES | review | **NO** | YES | YES | **NO** |
| Lifecycle error handling | YES | unit | **NO** | YES | YES | **NO** |
| Lifecycle tenancy (org/workspace) | **NO** (blocked B3) | — | — | — | — | — |
| Canonical membership provisioning | YES | YES (20) | n/a | YES | YES | YES (chain 7/7) |
| Login for tenant-less accounts | YES | YES (5) | **NO** | YES | YES | YES (HTTP 200 + session) |
| Workspace context resolution | YES | tests | **NO** | YES | YES | YES (200 + capabilities) |
| Workspace integration (canonical panel) | YES | — | **NO** | YES | YES | **NO** |
| Certification tenant | YES | chain proven | n/a | n/a | n/a | YES (isolated) |
| Document intelligence | NO | NO | NO | — | NO | NO |
| Living presence | NO | NO | NO | — | NO | NO |
| Responsive certification | NO | NO | NO | — | NO | NO |

**Document intelligence must NOT start** — gated behind Content + Workspace PASS.
**Responsive/Living must NOT start** — same gate.

---

## PRODUCTION EVIDENCE (deployed SHA 604cf29)

Authorization chain for the isolated certification tenant
(`scripts/provision_certification_tenant.py`) — **7/7 PASS**:
identity → organization, identity → active membership, membership → workspace,
workspace → organization, `authorized_workspace_ids` resolves exactly it,
`assert_object_access` ALLOWS it, and DENIES an unauthorized workspace.

Live HTTP with the certification session:
`POST /login` → 200 `{"success": true}` + session cookie;
`GET /api/v1/workspace/context` → 200 with capabilities;
`GET /api/v1/content/history` → 200 truthful empty.

Lifecycle journey (deployed app, production DB) —
`scripts/verify_content_lifecycle_journey_prod.py` — **18/18 PASS**, and
production rows left unchanged (self-cleaning verified).

---

## OPEN ITEMS

### B1 — Browser certification credential (BLOCKING the gate)
The vault has no login for shunyaos.com. This turn the vault save was
**declined**, so the password could not be filled and no browser session could
be established. Credential for the certification tenant is staged at
`/home/shunya-deploy/.shunya/certification_login.txt` (0600, not in git).
**To resume:** save that login in the vault, then run the browser journey.

### B2 — P0: existing production users remain fail-closed (INTENTIONAL)
`sh_workspace_memberships` has 0 rows for all pre-existing users, and nothing
writes it for them. Every `assert_object_access` caller fails closed for them
(`app/objects/routes.py`, `objects/canonical.py`, `file_routes.py`,
`app/pdf/routes.py`, `app/automation/service.py`,
`app/production/identity/onboarding_routes.py`, `core/object_service.py`).
Per the founder ruling this stays fail-closed until an audited
membership-provisioning/migration decision. **The certification tenant does NOT
solve this globally — it is explicitly out of scope.**

### B3 — ContentGeneration tenancy (blocked on B2)
See `R6B-2.7_CONTENTGENERATION_OWNERSHIP_DECISION.md`. Add
`organization_id`/`workspace_id` and delegate to `assert_object_access` once B2
is ruled.

### B4 — Other latent defects found (not yet fixed)
- `GET /api/v1/reality/stream` errors and workers are SIGKILLed (OOM).
- 344 of 483 `sh_objects` rows are `object_type='test'` — test pollution in
  production.
- `scripts/seed_panchi_club_demo.py` contains a **database credential in a
  tracked file**.
- `spc_default` exists in `sh_workspaces` with `organization_id` NULL despite
  being named as a forbidden synthetic workspace.

---

## EXACT NEXT ACTION

1. **B1 (browser certification)** — with the certification login in the vault:
   Workspace → Content Studio → existing content → Archive → Restore → Trash →
   Recover → Permanent Delete, including the confirmation cancel path and
   repeat-delete not-found. Then the Workspace entry path (§9).
2. Only after Content + Workspace PASS: Document Intelligence (§11–12), then
   Living presence (§13), Responsive (§14), full regression + PostgreSQL
   certification, delivery gate (§17).
3. Obtain the B2 ruling when the founder is ready; it gates real-user access to
   every workspace-scoped capability.