# R6B-2.7 — WINDOW 5 EXECUTION STATE (persistent resume ledger)

**This is an execution-resume state, NOT a certification report.**
A future execution turn MUST start by reading this file plus `git status`, then
continue at "EXACT NEXT ACTION" without re-reading the conversation history.

> WINDOW 5 STATUS: **IN PROGRESS** — not PASS.
> The status model is the six-level one (IMPLEMENTED / TESTED / BROWSER VERIFIED /
> CI VERIFIED / DEPLOYED / PRODUCTION VERIFIED). No single DONE label is used.

---

## CHECKPOINT — 2026-09-17

- **Repo:** `/home/shunya-deploy/shunya_os`
- **HEAD:** f4d903f (local == origin/master, working tree clean)
- **Branch:** master
- **CI:** run 35195181453 — test **success**, deploy **success**
- **Production SHA:** f4d903f (`release_type=CI_CERTIFIED`,
  `release_health_verified=true`, deployed 2026-09-17T07:51:11Z)
- **DB migration head:** `r6b27_content_lifecycle` (applied)
- **Previous checkpoint:** 322abe8 (whose CI was FAILED — see truth correction)

SHA chain (oldest → newest):
`ace57bb → 00ad624 → 7c6af3a → 1064a11 → 8f0f182 → 233f351 → cad9321 → 53b149f
 → 322abe8 (CI FAILED) → f4d903f (CI SUCCESS, deployed)`

---

## TRUTH CORRECTION (this session)

The prior version of this ledger claimed the Content Studio lifecycle was DONE
and Workspace integration was verified. Both claims were WITHDRAWN.
Full evidence: `R6B-2.7_WINDOW_5_TRUTH_CORRECTION.md`.

Verified defects found and fixed in Commit A (f4d903f):

1. Production DB was missing `status` + `is_deleted` on `m6_content_generations`
   — no migration ever created them. Every read/lifecycle call raised
   `UndefinedColumn`. Migration `r6b27_content_lifecycle` added them; applied.
2. The lifecycle transition table used `setattr` lambdas (always return `None`),
   so **every** action returned 400 "Invalid transition" — the feature had never
   worked. Replaced with an explicit state machine.
3. `GET /history` swallowed storage errors and returned
   `{"success": true, "data": []}` — a fake empty library. Now a truthful 500.
4. Permanent delete had no confirmation. Now requires a deliberate,
   irreversibility-stating confirmation.
5. Frontend lifecycle actions failed silently and mutated the list
   optimistically. Now truthful, with no optimistic mutation.
6. Dead `apiDeleteItem` hard-DELETE helper removed (also the TS6133 error that
   was failing CI at 322abe8).

---

## SIX-LEVEL STATUS

| Requirement | IMPLEMENTED | TESTED | BROWSER VERIFIED | CI VERIFIED | DEPLOYED | PRODUCTION VERIFIED |
|---|---|---|---|---|---|---|
| Lifecycle schema (status/is_deleted) | YES | YES | n/a (schema) | YES | YES | YES |
| Lifecycle API state machine | YES | YES (22 tests) | NO | YES | YES | YES (18/18 HTTP journey) |
| Lifecycle history read (truthful) | YES | YES | NO | YES | YES | YES |
| Frontend lifecycle controls | YES | typecheck+lint+build | NO | YES | YES | NO |
| Permanent-delete confirmation | YES | code review | NO | YES | YES | NO |
| Lifecycle error handling | YES | unit | NO | YES | YES | NO |
| Lifecycle tenancy (org/workspace) | **NO** | — | — | — | — | — |
| Workspace integration (canonical panel) | YES | — | **NO** | YES | YES | NO |
| Canonical workspace authorization | YES (code) | test-only | NO | YES | YES | **NO — 0 memberships** |
| Document intelligence | NO | NO | NO | — | NO | NO |
| Living presence | NO | NO | NO | — | NO | NO |
| Responsive certification | NO | NO | NO | — | NO | NO |

**Document intelligence must NOT start** — the directive gates it behind
Content + Workspace PASS.

---

## PRODUCTION EVIDENCE (deployed SHA f4d903f)

`scripts/verify_content_lifecycle_journey_prod.py` — **18/18 checks passed**
against the production application and production PostgreSQL:

- archive ACTIVE→ARCHIVED, restore ARCHIVED→ACTIVE
- trash ACTIVE→TRASHED, recover TRASHED→ACTIVE
- invalid transition → 409 with the current state
- permanent_delete blocked from ARCHIVED, allowed from TRASHED, row removed
- repeat permanent_delete → truthful 404
- cross-identity lifecycle → 404 (no existence disclosure), row untouched
- unauthenticated → 401
- production rows unchanged (self-cleaning verified)

---

## OPEN BLOCKERS (need a founder decision — not inferable)

### B1 — Browser certification credential
BROWSER VERIFIED cannot be reached without an authenticated session, and no
login is stored in the vault. Real browser certification of the Content Studio
lifecycle and the Workspace entry path requires a certification login.
**Decision needed:** provide a login for an existing identity, or authorise
provisioning of a dedicated certification identity (new org + workspace +
membership, no changes to existing data).

### B2 — Canonical workspace authorization has NO production data (P0)
`sh_workspace_memberships` = **0 rows**, and **nothing in the codebase writes to
it** (only the model definition and the read path). Every caller of
`assert_object_access` therefore fails closed for every real user:
`app/objects/routes.py`, `app/objects/canonical.py`, `app/objects/file_routes.py`,
`app/pdf/routes.py`, `app/automation/service.py`,
`app/production/identity/onboarding_routes.py`, `core/object_service.py`.
Four production workspaces exist: `spc_business` (org 7), `spc_personal` (org 1),
`spc_custom` (org 1), `spc_default` (org NULL).
This is the data blocker already recorded in
`artifacts/r6b25/R6B-2.6_WORKSPACE_AUTH_ARCHITECTURE.md` §5.
**Decision needed:** membership semantics — which identities belong to which
workspaces. Deriving this from `org_members` would be inventing authorization
and is forbidden.

### B3 — ContentGeneration is not yet a certified second authority
Recorded in `R6B-2.7_CONTENTGENERATION_OWNERSHIP_DECISION.md`. It is currently
identity-scoped with no tenancy, so it is not a canonical object authority. The
follow-up (add `organization_id`/`workspace_id`, enforce `assert_object_access`)
is **blocked on B2**.

---

## EXACT NEXT ACTION

1. **Unblock B1** (certification login) and run the browser certification of:
   Workspace → Content Studio → existing content → Activate/Archive/Trash/
   Restore/Recover/Permanent Delete, including the confirmation cancel path
   (cancel → nothing deleted) and repeat-delete not-found.
2. Then verify the Workspace entry path in the browser (§9): sidebar
   `Content` nav item → `PrimaryWorkspace` panel (this is the canonical surface;
   `living-workspace.tsx` is dead code and must not be cited as integration).
3. Only after Content + Workspace PASS: Document Intelligence (§11–12).
4. Then Living presence (§13), Responsive (§14), full regression (§15),
   PostgreSQL certification, delivery gate (§17).
