# R6B-2.7 — WINDOW 5 TRUTH CORRECTION

**Date:** 2026-09-17
**Author:** Hermes (execution engine) — this is a TRUTH RECONCILIATION, not a certification.
**Directive:** WINDOW 5 TRUTH CORRECTION + PRODUCT EXECUTION

> The previous Window 5 ledger (`R6B-2.7_WINDOW_5_EXECUTION_STATE.md`) is NOT trusted.
> Every claim below was re-derived from live Git, live CI, live production DB and live HTTP.

---

## 1. GIT / CI / PRODUCTION TRUTH

| Dimension | Reported by prior session | ACTUAL (this session, verified) |
|---|---|---|
| Repo path | (not stated) | `/home/shunya-deploy/shunya_os` |
| HEAD | cad9321 | **322abe8** |
| origin/master | (implied equal) | **322abe8** (equal, 0 ahead / 0 behind) |
| Working tree | clean | **clean** (0 entries) |
| Production SHA (health) | "8f0f182" and "00ad624" in the same file | **322abe8** |
| CI for HEAD | "in_progress" | **FAILURE** (run 35144932068) |
| Deploy job | (implied deployed) | **SKIPPED** — gated on failing test job |
| release_type | (implied CI_CERTIFIED) | **UNVERIFIED** — "Deployment evidence does not match the running build" |

The reported SHA chain is **real and in order** (oldest→newest):
`ace57bb → 00ad624 → 7c6af3a → 1064a11 → 8f0f182 → 233f351 → cad9321 → 53b149f → 322abe8`

**Corrections:**
- The prior ledger contradicts itself on production SHA (states `00ad624` at line 15 and `8f0f182` at line 18).
- Last SHA with a GREEN CI run is **8f0f182**. Runs for 1064a11, 233f351, cad9321, 53b149f were **cancelled** (superseded). 322abe8 is **FAILED**.
- **Production is running 322abe8 — a SHA whose CI failed and whose deploy job never ran.**
  Production was therefore advanced out-of-band (repo checkout + service restart), not by CI.
  The health endpoint is truthfully reporting `UNVERIFIED`.

**CI failure — exact cause:**
```
src/components/content/content-studio.tsx(190,16): error TS6133:
  'apiDeleteItem' is declared but its value is never read.
```
Single blocking error. Backend suite, compile, verification tests all PASSED.

---

## 2. CONTENT STUDIO LIFECYCLE — PRODUCTION REALITY

### 2.1 Production database is missing the lifecycle schema (BLOCKER)

Live production DB `m6_content_generations` columns:
```
id, identity_id, content_type, platform, prompt, generated_content,
tone, target_audience, word_count, ai_model, is_favorited, created_at
```
Model `ContentGeneration` declares **two more**: `status`, `is_deleted`.

- No migration ever adds them. `migrations/versions/0010_schema_reconciliation.py` only
  CREATEs the table without them. No `ADD COLUMN` anywhere. No runtime auto-migration.
- Alembic head in production = `r6b27_workspace_memberships` (current, does not include them).

**Read-only ORM proof (scripts/verify_content_lifecycle_prod.py), against the LIVE database:**
```
MISSING IN DB: ['status', 'is_deleted']
ENDPOINT_QUERY: FAILED -> ProgrammingError
(psycopg2.errors.UndefinedColumn) column "status" does not exist
```

**Consequence:** every Content Studio history read and every lifecycle action raises
`UndefinedColumn` in production.

### 2.2 The history endpoint reports FAKE SUCCESS

`app/content_studio/routes.py:109-111`
```python
except Exception as e:
    logger.warning("Content history error: %s", e)
    return jsonify({"success": True, "data": [], "total": 0})   # <-- fake success
```

The `UndefinedColumn` error is swallowed and the endpoint returns **HTTP 200,
`success: true`, empty list**. The user's 3 existing content items are invisible and
they are told (by silence) that nothing is wrong.

This directly violates the directive §8 ("must not silently fail") and §6 ("no fake success").

### 2.3 Lifecycle API — implemented, but authorization is identity-only

`POST /api/v1/content/history/<id>/lifecycle` (app/content_studio/routes.py:169)

- Authorization test: `item.identity_id != _identity_id()` → 404 (correctly non-disclosing).
- **There is NO organization and NO workspace dimension.** `ContentGeneration` has no
  `organization_id` / `workspace_id` column and none is consulted.
- Therefore "wrong workspace" and "wrong organization" lifecycle tests **cannot be
  meaningfully performed** — the model has no tenancy. Same identity in a different
  workspace/org still sees and mutates the same rows.

### 2.4 Frontend lifecycle controls

- Buttons exist, context-aware (ACTIVE→Archive+Trash, ARCHIVED→Restore+Trash,
  TRASHED→Recover+PermanentDelete). Calls `/api/v1/content/history/<id>/lifecycle`.
- **Permanent Delete has NO confirmation step** — direct `onClick`
  (`content-studio.tsx:1526`). Violates directive §7.
- **Silent failure on error** (`content-studio.tsx:660-671`): the handler updates UI only
  when `resp.ok`; on failure nothing is shown — no error, no retry.
- `apiDeleteItem` / `apiUpdateItem` are legacy leftovers; one is unused → CI failure.
- `apiDeleteItem` targets `DELETE /api/v1/content/history/<id>` which is a *hard* delete
  that bypasses the trash requirement — a competing deletion path.

---

## 3. CONTENTGENERATION vs sh_objects — ARCHITECTURAL QUESTION

| | `ContentGeneration` (`m6_content_generations`) | Canonical object (`sh_objects`) |
|---|---|---|
| Tenancy | `identity_id` only | `organization_id` + `workspace_id` |
| Lifecycle | `status` + `is_deleted` (own state machine) | `status` + `is_deleted` via `ObjectService` |
| Service path | direct ORM, blueprint route | `ObjectService` (single authority) |
| Authorization | identity equality only | persisted ownership + workspace membership |

**Verdict: NOT converged. This is currently a SECOND, WEAKER object authority for content.**

- It duplicates lifecycle semantics that the canonical `ObjectService` already owns.
- It carries **no tenancy**, so it cannot honour the workspace/organization authorization
  model that Window 5 established as canonical.
- `ContentGeneration` does not reference an `sh_objects` row.

Decision and justification are recorded in §7 (Remediation Decision).

---

## 4. WORKSPACE INTEGRATION — VERIFIED BY CODE GRAPH (browser verification pending)

The prior ledger claimed integration via a "Living Workspace top-bar ✗ Content button → modal overlay".

**That claim is FALSE.** `frontend/src/components/living-workspace/living-workspace.tsx`
(exports `LivingWorkspace`, contains the Content Studio modal at line 271-279) is
**never imported anywhere in the application**. `grep -rn "living-workspace"` outside its
own directory returns only `living-store` (a different, used module). The modal component
is **dead code**.

**What is actually true:**
- The authenticated landing surface is `PrimaryWorkspace` (`executive-home.tsx`), rendered
  unconditionally by `AuthenticatedWorkspace` (app.tsx:486-491).
- The sidebar defines a canonical nav item: `{ id: 'content', label: 'Content', wsType: 'object' }`.
- `executive-home.tsx:1026` — `if (active.identity.objectId === 'content') return <ContentStudio />`
  inside the persistent workspace shell. Content Studio is mounted **lazily** (line 49).

So the canonical Workspace surface for Content **does** exist — but the ledger justified it
with a dead component, which is not evidence. Whether it satisfies the Product Constitution
requires **actual browser verification** (directive §9). Not yet performed.

`/workspace/content/content` does not exist as a route. Per directive §10 this is **not**
required, and no such route is to be invented.

---

## 5. CANONICAL WORKSPACE AUTHORIZATION HAS NO PRODUCTION DATA

| Table | Rows in production |
|---|---|
| `organizations` | 159 |
| `org_members` | 167 |
| `sh_workspaces` | 4 |
| `sh_objects` | 483 |
| **`sh_workspace_memberships`** | **0** |

`resolve_current_workspace()` calls `authorized_workspace_ids(identity_id, organization_id)`,
which reads `sh_workspace_memberships`. With **zero rows**, that returns an empty set and
the resolver **fails closed** (`no authorized workspace`).

The prior ledger claims "Workspace resolution ... Membership in `sh_workspace_memberships`"
as a verified Window 5 criterion. That is true only as *code*; in **production the table is
empty**, so the canonical workspace path cannot authorize any real user.

---

## 6. STATUS CORRECTION — SIX-LEVEL MODEL (no single DONE)

| Requirement | IMPLEMENTED | TESTED | BROWSER VERIFIED | CI VERIFIED | DEPLOYED | PRODUCTION VERIFIED |
|---|---|---|---|---|---|---|
| Lifecycle API (archive/restore/trash/recover/permanent_delete) | YES | SQLite tests only | NO | **FAILED** | NO (schema absent) | **NO — query errors** |
| Lifecycle DB schema | model only | — | — | — | **NO** | **NO** |
| Content Studio history read | YES | SQLite only | NO | FAILED | NO | **NO — fake empty success** |
| Frontend lifecycle controls | YES | NO | NO | **FAILED (TS6133)** | NO | NO |
| Permanent-delete confirmation | **NO** | NO | NO | — | NO | NO |
| Lifecycle error handling (truthful) | **NO** | NO | NO | — | NO | NO |
| Lifecycle tenancy (org/workspace) | **NO** (model has none) | NO | NO | — | NO | NO |
| Workspace integration (canonical panel) | YES (code) | NO | **NO** | FAILED | NO | NO |
| Workspace integration (modal) | dead code | — | N/A | — | — | N/A |
| Canonical workspace authorization | YES (code) | tests only | NO | FAILED | NO | **NO — 0 memberships** |
| Document intelligence | NO | NO | NO | — | NO | NO |
| Living presence | NO | NO | NO | — | NO | NO |
| Responsive certification | NO | NO | NO | — | NO | NO |

**Prior ledger's "Content Studio lifecycle — DONE" and "Workspace/Home integration — verified"
are hereby WITHDRAWN.** They described implementation progress, not product certification.

---

## 7. REMEDIATION DECISION (Commit A — lifecycle remediation)

**A1. Schema.** Add an Alembic migration that adds `status` (default `'active'`, NOT NULL)
and `is_deleted` (default `false`) to `m6_content_generations`, with an index on
`(identity_id, status)`. Apply to production. *Without this, nothing else can be certified.*

**A2. Truthful errors.** Replace the swallow-and-return-empty block with a truthful failure
(`success: false`, HTTP 500) so the UI can show real state. No fake success.

**A3. Permanent-delete protection.** Require an explicit confirmation in the UI that states
irreversibility (directive §7), using the existing SHUNYA interaction pattern.

**A4. Frontend error handling.** Surface failure state, do not optimistically remove items,
offer retry.

**A5. Remove competing deletion path.** `apiDeleteItem` (hard DELETE) must not coexist with
the lifecycle. Remove dead code (fixes TS6133).

**A6. ContentGeneration authority decision.** See §8.

---

## 8. CONTENTGENERATION OWNERSHIP BOUNDARY (decision)

To be finalised with evidence in the Commit A report. Constraint already established:

- Convergence onto `sh_objects` is the architecturally correct end-state **if** content is a
  canonical SHUNYA object.
- A domain-specific table is legitimate **only if** its lifecycle is independently
  authoritative *and* authorization is equal in strength to the canonical path (tenancy).

Since `ContentGeneration` currently carries **no tenancy**, separation is **not** currently
legitimate. The direction of travel is convergence-or-tenancy, decided on evidence, not wording.

**No second permanent object authority will be left in place.**

---

## 9. EXACT NEXT ACTION

1. Commit A: migration (A1) + truthful errors (A2) + confirm-delete (A3) + error handling (A4)
   + dead-code removal (A5) + authority decision (A6).
2. Fix the CI blocker so CI can go green.
3. Commit → push → CI → deploy → production SHA → health.
4. Browser-certify the Content Studio lifecycle journey against production.
5. Then Workspace (§9 of directive) → Document Intelligence → Living → Responsive → Regression.
