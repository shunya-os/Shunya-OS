# Constitutional Discovery Report — CDR-004
## Command Surface Consolidation

**CEP:** 004
**Status:** Discovery Complete
**CEP Objective:** Eliminate duplicate command surfaces. 4 command surface implementations → 1 canonical + 1 complementary (palette). Satisfies Canonical Ownership Law §10, Simplicity Rule §4.
**EX-02 Compliance:** Infrastructure topology included. Constitutional Evidence Boundary declared.

---

### 1. Constitutional Evidence Boundary

| Scope | Detail |
|-------|--------|
| **Audited** | All command surface implementations in `frontend/src/components/` |
| **Not audited** | AI Agent system, voice recognition pipeline, living store internals (used, not owned) |
| **Proven** | `executive-home/command-surface.tsx` consumed by `executive-home.tsx` only. `ai/command-bar.tsx` consumed by `homepage.tsx` only. |
| **Supported by evidence** | Canonical `living-workspace/command-surface.tsx` is independent — tied to living-store context, not a drop-in replacement |

---

### 2. Infrastructure Topology

#### 2.1 Command Surface 1: `living-workspace/command-surface.tsx` (CANONICAL)

| Property | Value |
|----------|-------|
| **File** | `frontend/src/components/living-workspace/command-surface.tsx` |
| **Lines** | 231 |
| **Exports** | `CommandSurface` (default FC) |
| **Purpose** | Contextual suggestions based on living store (observations, objects, executions). Framer-motion. No CRUD, no AI chat, no global search. |
| **Consumers** | `living-workspace/living-workspace.tsx` (1 consumer) |

#### 2.2 Command Surface 2: `executive-home/command-surface.tsx` (LEGACY E1)

| Property | Value |
|----------|-------|
| **File** | `frontend/src/components/executive-home/command-surface.tsx` |
| **Purpose** | `/open`, `/create`, `/new`, voice recognition, AI intelligence query |
| **Consumers** | `executive-home/executive-home.tsx` (1 consumer) |
| **Re-exported by** | `executive-home/index.ts` (line 2) |

#### 2.3 Command Surface 3: `ai/command-bar.tsx` (DUPLICATE)

| Property | Value |
|----------|-------|
| **File** | `frontend/src/components/ai/command-bar.tsx` |
| **Lines** | 1,472 |
| **Purpose** | Multi-step workflows, undo stack, context memory, scheduling, global search, full CRUD, AI chat |
| **Consumers** | `components/public/homepage.tsx` (1 consumer) — rendered inside `<AICommandBar onOpenProposal={...} onExecuted={...} onNavigate={...} />` |
| **Risk** | Largest file in scope (1,472 lines). Significant functionality. |

#### 2.4 Command Surface 4: `ai/command-palette.tsx` (COMPLEMENTARY — KEEP)

| Property | Value |
|----------|-------|
| **File** | `frontend/src/components/ai/command-palette.tsx` |
| **Purpose** | Mantine Spotlight Cmd+K — navigation actions, open objects, search |
| **Consumers** | Used via `useCommandPalette()` hook. No direct import. |

---

### 3. Consumer Import Graph

```
executive-home/command-surface.tsx (LEGACY)
  ├── executive-home/executive-home.tsx (imports CommandSurface, renders it)
  │   └── workspace/workspace-container.tsx (imports ExecutiveHome, renders when no active workspace)
  └── executive-home/index.ts (re-exports CommandSurface — only re-export of same file)

ai/command-bar.tsx (DUPLICATE)
  └── public/homepage.tsx (imports AICommandBar, renders with props)

living-workspace/command-surface.tsx (CANONICAL)
  └── living-workspace/living-workspace.tsx (imports CommandSurface, renders)

ai/command-palette.tsx (COMPLEMENTARY — KEEP)
  └── hooks/workspace-hooks.ts (useCommandPalette — used by executive-home.tsx via hook)
```

---

### 4. Canonical Owner Confirmation

| Role | Owner | Evidence |
|------|-------|----------|
| **Canonical Command Surface** | `living-workspace/command-surface.tsx` | LX-01 canonical. Framer-motion, living-store integration, contextual suggestions. |
| **Legacy** | `executive-home/command-surface.tsx` | E1 milestone, being replaced by LX-01. Consumers: 1 (executive-home.tsx) |
| **Duplicate** | `ai/command-bar.tsx` | Overlaps with living command surface. 1,472 lines — significant scope. |
| **Complementary** | `ai/command-palette.tsx` | Mantine Spotlight Cmd+K — different UX pattern. Keep. |

---

### 5. Migration Sequence

```
Step 1: Remove AICommandBar from homepage.tsx
  - Remove import and <AICommandBar> JSX element from homepage.tsx
  - Verify no compile errors
  - Verify homepage renders without regression

Step 2: Remove components/ai/command-bar.tsx
  - Delete the file (now zero consumers after Step 1)

Step 3: Remove CommandSurface from executive-home.tsx
  - Remove import and usage of CommandSurface in executive-home.tsx
  - Verify executive-home.tsx still renders (the command surface was a feature within it)

Step 4: Remove components/executive-home/command-surface.tsx
  - Delete the file (now zero consumers after Step 3)
  - Update executive-home/index.ts to remove the re-export

Step 5: TypeScript compile + build + test suite
```

**Note:** The canonical `living-workspace/command-surface.tsx` is NOT used as a replacement. It is a fundamentally different component tied to the living-store context. The CEP objective is to **remove** the two non-canonical command surfaces (executive-home command-surface and ai/command-bar) rather than merge them into the canonical one. The canonical surface already serves its purpose for the LX-01 experience.

---

### 6. Rollback Sequence

```
Step 1: git revert <commit>
Step 2: Verify all 4 command surface files restored
Step 3: Verify homepage and executive-home render correctly
Step 4: Full TS compile + build
```

**Rollback confidence:** HIGH — each step is independently reversible. No schema, no data, no API.

---

### 7. Verification Plan

| Test | Method | Expected |
|------|--------|----------|
| command-bar.tsx removed | File check | Gone |
| command-surface.tsx (executive-home) removed | File check | Gone |
| command-palette.tsx unchanged | File check | Present |
| No references to AICommandBar | grep | 0 occurrences |
| No references to executive-home/command-surface import | grep | 0 occurrences |
| TypeScript compile | `tsc -b --noEmit` | Exit 0 |
| Frontend build | `vite build` | Exit 0 |
| Homepage renders | Manual check | Page loads without errors |
| Workspace container loads | Manual check | Fallback view renders |

---

### 8. Behaviour Preservation Matrix (EX-03)

| Behaviour | Status | Evidence | Constitutional Justification | Founder-visible Impact |
|-----------|--------|----------|------------------------------|------------------------|
| AI command execution | Removed | Proven — `ai/command-bar.tsx` provided AI chat, undo stack, scheduling, global search, CRUD | Simplicity Rule §4 — redundant with living-workspace command surface + command-palette. Homepage is legacy path being replaced by LX-01. No consumer of AICommandBar in canonical paths. | **Negative** — founder on the legacy homepage loses AI command bar. Founder on `/living` (LX-01) unaffected. This behaviour is intentionally removed from the legacy path to incentivize migration to `/living`. |
| Executive home command surface | Removed | Proven — `executive-home/command-surface.tsx` provided `/open`, `/create`, `/new`, voice | Simplicity Rule §4 — E1 milestone superseded by LX-01. executive-home.tsx is a fallback view with no active workspace, not a primary interface. | **Negative (low impact)** — founder on the fallback workspace view loses the dedicated command surface. The workspace container already renders ExecutiveHome text with status; the command surface was secondary. |
| Mantine Spotlight Cmd+K | Preserved | Proven — `command-palette.tsx` remains | N/A (kept) | None |

---

### 9. Risk Assessment

| Risk | Severity | Mitigation |
|------|----------|------------|
| Homepage loses AI command bar functionality | MEDIUM | Homepage is a legacy path being replaced by LX-01 (`/living`). The canonical living-workspace command surface and command-palette provide the LX-01 experience. Homepage users lose AI chat/CRUD/global search — this is intentional to drive migration to the canonical path. |
| executive-home.tsx breaks without CommandSurface | LOW | executive-home.tsx is 856 lines — the CommandSurface is one feature within it. Removing just the import+usage leaves the rest intact. |
| workspace-container.tsx fallback view degraded | LOW | workspace-container.tsx renders ExecutiveHome as a loading/fallback component. The command surface was peripheral. |
| Build failure | LOW | `tsc` catches all import errors |

---

### 10. Success Evidence

| # | Evidence | Measure |
|---|----------|---------|
| 1 | Canonical Ownership Law §10 satisfied | 4 command surfaces → 1 canonical + 1 kept (palette) |
| 2 | Simplicity Rule §4 satisfied | 2 files removed |
| 3 | 2 legacy consumers migrated | homepage.tsx no longer imports command-bar; executive-home.tsx no longer imports command-surface |
| 4 | TypeScript compiles | Exit 0 |
| 5 | Build succeeds | Exit 0 |
| 6 | Repository simplified | −2 files, −2 ownership points |

### 11. Remaining Unknowns

**No remaining architectural unknowns within CEP-004 scope.**

All four command surfaces traced, all consumers identified, migration order defined, rollback validated.

---

*CDR-004 produced by SHUNYA Constitutional Chief Architect*
*Conforms to: CAS-01, LX-06D-R2, LX-06D-R3, EX-02, EX-03*
**EX-03 Compliance:** Behaviour Preservation Matrix included (four states: Preserved/Enhanced/Redesigned/Removed). Every non-preserved behaviour carries constitutional justification and a founder-visible impact statement.
*Evidence levels: Proven (consumer import chains), Supported (component analysis), Inferred (migration pattern)*