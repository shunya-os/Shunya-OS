"""FOR-2 Organization Operating System — Data Models.

Canonical models moved to app.models. This file kept for backward compat
but contains no model definitions.
"""
# All canonical models are now in app.models
# Organization, OrgMember, OrgInvitation, Department imported via app.models