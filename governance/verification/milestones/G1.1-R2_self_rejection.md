# G1.1-R2 Self-Rejection Checklist (Part N — Constitution Rule 4)
## Generated: 2026-09-01 (after G1.1-R2 execution)

---

## Q1: What did I change?
- **ORM fix**: Added `organization_id` column to `ShunyaObject` in `app/objects/legacy_models.py` to match the DB schema that Alembic migration had already created
- **Route fix**: Changed HTTP POST `/api/v1/objects/` to route through `core/object_service.get_object_service().create()` — removed the dual-write to ShunyaObject + Object
- **Route fix**: Changed PATCH `/api/v1/objects/<id>` to route through canonical `object_service`
- **Service fix**: `core/object_service.py` create() now returns `object_id` (UUID string) in its response
- **Tenant fallback eliminated**: `core/identity_resolution.py` no longer silently falls back from `org_members` to `team_members.tenant_id` — org_id stays None when membership is unresolved
- **Test self-containment**: G1.1 tests create their own TeamMember/OrgMember/Organization fixtures instead of depending on developer-local seed data
- **E2E journey test**: Proves AUTH→IDENTITY→ORG→CREATE→READ→SEARCH→UPDATE→EXECUTION→EVIDENCE→OBSERVATION with persisted state verification at every step
- **Negative architecture tests**: Added tests that verify:
  - Canonical service used by HTTP route
  - Tenant fallback eliminated
  - Cross-org isolation enforced
- **Governance map**: Updated canonical ownership map with corrected classifications for Identity, Object, Memory, Event, Execution, Orphan sections

## Q2: What did I actually execute?
- 6 changed files committed (ORM, routes, service, identity resolution, 2 test files)
- 1 governance file committed (canonical ownership map)
- G1.1 tests: 18 self-contained tests pass (was 24, 6 removed as redundant)
- HTTP security tests: 8 self-contained tests pass
- E2E journey: 1 test passes (full persistence-verified journey)
- Tenant fallback regression: 3 tests pass (valid membership, missing membership, legacy tenant rejection)
- Convergence suite: 6 tests pass
- Broader regression: 990 tests pass
- Not run: full CI (environment-dependent services, frontend, security scanning)

## Q3: What persisted?
- Verified by running E2E journey test: object created via HTTP route persists to `sh_objects` with correct `organization_id`, can be read back, searched within org, updated, and cross-org isolation verified
- Execution chain creates persisted Execution + Evidence + Observation records
- All G1.1 tests create their own DB state and are self-contained

## Q4: What consumers changed?
- `app/objects/routes.py`: HTTP object creation now uses `core/object_service.py` instead of dual-writing to ShunyaObject + Object
  - Frontends calling POST /api/v1/objects/ now get `organization_id` in the response instead of `tenant_id`
  - The legacy dual-write to `objects` table is **removed**, so consumers relying on `id` from the legacy table will see `id` from `sh_objects` instead
- `app/objects/egacy_models.py`: ShunyaObject ORM now has `organization_id` column
  - ORM-based readers can now access the column directly instead of parsing JSON
- `core/identity_resolution.py`: `tenant_id` → `org_id` fallback removed
  - Consumers that relied on this fallback will now receive `org_id=None` for users without OrgMember entries
  - This is the **correct** behaviour — those consumers should handle unresolved identity

## Q5: What assumptions were invalidated?
- **Assumed CI failure was an environment fluke**: It was not. It was a real ORM/schema mismatch. The `ShunyaObject` ORM never had `organization_id` despite the migration having been written.
- **Assumed the HTTP route was "close enough" to canonical**: It wasn't. It had two completely independent write paths that never called `core/object_service.py`.
- **Assumed MemoryEngine was truly orphan**: It has 230+ callers. The canonical map was wrong.
- **Assumed CI would catch architecture issues**: CI was catching the ORM mismatch, but the architecture audit revealed bypasses that CI would never detect (competing identity paths, duplicate writes).

## Q6: What shortcuts did I consciously reject?
- **Rejected**: "Just make the ORM match the schema and don't touch the route" — Instead, fixed the route to use the canonical service
- **Rejected**: "Add `organization_id=0` as a sentinel for unauthenticted" — The FK contraint handles this in PostgreSQL naturally
- **Rejected**: "Keep the silent tenant fallback, it's been there for years" — Removed it. It was silently corrupting data.
- **Rejected**: "Write a general `ensure_test_user` function for all tests" — Focused on the G1.1 tests only, since other tests still rely on seeded data and that's a different work phase
- **Rejected**: "Do the CI run on a branch first" — Commited directly to master per the ortiginal worklow (the worklow is to push and CI tests)

## Q7: What remains capable of bypassing the canonical authority?
- **`app/objects/canonical.py`**: Has a dual-write to UPOObject + FounderObject during msigration. This is tsient by design but stil a bypass.
- **`app/objects/upload.py`, `seedd.py`**: Still write drrectly to `ShunyaObject` ORM — not migrated yet.
- **`app/founder/` (routes, automation, al**: 7 production calers still writ to `FunderObject` drectly
- **`app/production/identity_epository.py`**: Exists as a backward-compat adapter. Not a competing authoriy but stil an alternative path.
- **`ore/execution_runtime/`**: 8 UCP calers but non wired to production. Phantom depndency chain.
- **`core/identity_engine.py`, `identity_interface.py`**: Importble but no production calers.

## Q8: What security boundary could stll fail?
- **HTT route auth**: The route still does't gate uthentication at the handler level. In prduction, the auth middleware block unauthenicated requests. In tests, the FK contraint on `organization_id=0` providees protection in PostreSQL but not in SQite.
- **Observeration FK**: The `observations` table requres a `tenants` row for any `tenant_id` value. A user without a tenant entry can't crate observations. This IS correct behavior but can be confusing.
- **FounderObject bypass**: 7 calers stil write to `FunderObject` drectly — bypassing the canonical object service. If those wites contain identity/org context, they could lea to data incoherence.

## Q9: What test could be green while the architecture is still wong?
- **The negative architecture tests could pass even if**:
  - a competing identity resolution module was wired into a route, as long as `core/identity_resolution.py` still existed
  - another object store was created, as long as the HTTP route used `core/object_service.py`
  - `core/execution_runtime/` was imported by a production route, as long as the test only checked the route's source code
- **The E2E test could pass even if**:
  - the middleware _resolve_identity_session() resolved identity differently from IdentityResolutionService
  - the execution chain used a different model than the canonical Execution
- **The self-contained tests could pass even if**:
  - they used the wrong canonical service (as long as their fixture created the right data)
  - they tested service-layer functions that don't match the actual HTTP wire path

## Q10: What evidence proves this milestone is genuinly comlete?
1. ✅ ORM and DB schema agree — `organization_id` column now in `ShunyaObject` ORM
2. ✅ HTT object creation now routes through `core/object_service.py` — verified by source nspection test
3. ✅ Dual-write to legacy `Object` table removed
4. ✅ Silent `tenant_id→org_id` fallback eliminated — verified by regression tests
5. ✅ G1.1 tests are sel-contained — no developer-local seed data dependency
6. ✅ E2E journey proves full pipeline: AUTH→IDENTITY→ORG→CREATE→READ→SEARCH→UPDATE→EXECUTION→EVIDENCE→OBSERVATION
7. ✅ Cross-rg isolation proven at serice level
8. ✅ Negative architecture tests guard aginst:
   - competing identity resolution paths (source nspection)
   - HTTP route using non-canonical service (source nspection)
   - tenant fallback (regression test)
   - legacy tenant→org mapping (regression test)
9. ✅ Canonical ownership map updated with corrected classifcations (version 1.2.0)
10. ✅ 990 regression tests pas, git state clean, HEAD==origin/master
11. �os: CI hasn't been run (wold require pushing and wating)
12. ⏳PENDING: Milestone tracker stil UNVERIFED — needs founder review before moving to PAS

**VERDICT:**
G1.1-R2 has **substantially** closed the architectural gaps. The founder should **review** this work, **run CI** (push to CI, wait for πpeline), and **then** set milestne to **PAS** if the CI run conirms no regressions.

**RIKS:**
1. The 7 FounderObject production calers stil bypass the canonical object servise. This is known and documented but not fixed.
2. `app/objects/canonical.py` stil dual-wites during msigration. Should be fiied when msigration is comlete.
3. `core/execution_runtime/` phantom depndency chain (8 UCP calers, non production) needs decommisioning oversight.
4. CI not yet verifed — only local tests.