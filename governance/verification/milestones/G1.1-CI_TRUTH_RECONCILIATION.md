# G1.1-CI Truth Reconciliation

**Directive:** 10 §2
**Date:** 2026-09-10

## Commit History

| SHA | Message | CI Result | Cause |
|-----|---------|-----------|-------|
| e4c3afa | r6b: final CI test fixes — 222→0 | FAILURE (run #34310798521) | 195 test failures (pre-existing RBAC regressions) |
| e36e1db | R6B-2: establish canonical object convergence | CANCELLED (run #34402726653) | Superseded by subsequent push |
| 0780ebb | R6B-2: governance artifacts | FAILURE (run #34402749459) | **R6B-2-INTRODUCED** — conftest guard rejected all PostgreSQL (exit 4) |
| e48ab17 | R6B-2: fix test isolation guard | PENDING | Guard now permits CI test PG (port 5433) |

## Failure Classification

### e36e1db — Cancelled
- **Root cause:** Superseded by subsequent push (concurrency group cancelled in-progress runs)
- **Classification:** NOT A FAILURE — cancelled by new push
- **Action:** None required

### 0780ebb — Failure
- **Root cause:** `tests/conftest.py` guard rejected ALL PostgreSQL connections, including CI's provisioned test PostgreSQL at port 5433. Guard also required `DATABASE_URL` to be set, which CI's test step does not set.
- **Classification:** **R6B-2-INTRODUCED** — the conftest guard was added in checkpoint e36e1db but was too aggressive (rejected all postgresql:// URLs including CI test DB)
- **Evidence:** CI workflow provisions PostgreSQL at port 5433 (user=shunya, db=shunya_test_prod06). Guard matched `postgresql://` pattern and blocked. `--log-failed` returned exit code 4 (pytest import error).
- **Action:** ✅ Fixed in e48ab17 — guard now permits:
  - CI test PG (port 5433) 
  - Remote/test PG with `_test` database name
  - Still rejects production PG (port 5432 + shunya/shunya_os)
  - Still rejects missing DATABASE_URL

### e48ab17 — Pending
- CI run should now pass the import step. Actual test failures (if any) will need individual classification against the e4c3afa baseline.

## Summary

| Commit | Run | Failure Classification |
|--------|-----|----------------------|
| e36e1db | 34402726653 | CANCELLED (not a failure) |
| 0780ebb | 34402749459 | **R6B-2-INTRODUCED** — conftest guard regression |
| e48ab17 | pending | Waiting for CI |