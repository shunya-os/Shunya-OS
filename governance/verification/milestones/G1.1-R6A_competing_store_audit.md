# G1.1-R6A — Competing Store Audit (Identity / Workspace / Business Objects)

**Audit date:** 2026-09-08
**Agent:** deleg_cbce4bd1 (read-only)
**Authority:** R6A directive §13-15 — identity, workspace, business object convergence
**No code was modified in this audit.**

---

## Identity Stores

| Store | Table | Callers | Writes? | Classification |
|-------|-------|---------|---------|---------------|
| **shunya_identities** | `shunya_identities` | 10 | YES (identity_repository.py) | **CANONICAL** — identity profile |
| **persons** | `persons` | 36 | YES (identity/service, crm, auth_routes) | **CANONICAL** — human entity |
| **team_members** | `team_members` | 30 | YES (auth_routes, invitation, user_routes) | **CANONICAL** — authentication domain |
| **org_members** | `org_members` | 24 | YES (auth_routes, org_routes, invitation) | **CANONICAL** — org membership |
| **person_identities** | `person_identities` | 3 | YES (identity/service) — empty | **LEGACY** — FDA4 claim trail |
| **IdentityEngine** | (Knowledge Store) | 11 | YES — non-SQL | **QUARANTINED** — self-declared duplicate |

**Assessment:** 6 stores, but only `person_identities` and `IdentityEngine` are genuinely competing — the 4 "canonical" stores each serve distinct domains (profile vs human entity vs auth vs membership). **No decommission required for identity.**

## Workspace Stores

| Store | Table | Callers | Writes? | Classification |
|-------|-------|---------|---------|---------------|
| **FounderSpace** | `founder_spaces` | 16 | YES (founder/routes) | **CANONICAL** — provisional |
| **sh_workspaces** | `sh_workspaces` | 6 | NO | **LEGACY** — read-only |
| **user_workspaces** | `user_workspaces` | 3 | NO | **LEGACY** — minimal adoption |

**Assessment:** 3 stores, 1 canonical. Legacy stores are no longer written to. **No convergence action required.**

## Business Object Stores

| Store | Table | Callers | Writes? | Classification |
|-------|-------|---------|---------|---------------|
| **sh_objects** | `sh_objects` | 15 | YES (core/object_service — canonical write path) | **CANONICAL** |
| **founder_objects** | `founder_objects` | 21 | **YES** — 5 active production sites | **DUPLICATE** — actively written but should migrate to sh_objects |
| **objects** | `objects` | 19 | YES (objects/service — execution engine domain) | **LEGACY** — execution engine state machines |
| **entities** | `entities` | 24 | YES (api/entity_routes) | **LEGACY** — generic entity abstraction |
| **sh_uop_objects** | `sh_uop_objects` | 5 | YES (data_migration, kernel/routes) | **QUARANTINED** — migration compat |

**Critical gap:** `founder_objects` is the only store that is actively written to from production routes AND has a canonical alternative (`sh_objects`). The other legacy stores serve distinct domains.

### founder_objects write sites (must migrate to sh_objects):

1. `founder/routes.py:456` — `api_create_object`
2. `automation/service.py:248` — `_execute_action`
3. `ai/routes.py:335` — `chat`
4. `onboard.py:81` — `onboard`
5. `onboarding_routes.py:134` — `mark_onboarding_complete`

---

## Summary

| Area | Total Stores | Truly Competing | Action Required | Blocks G1.1-FINAL? |
|------|-------------|-----------------|----------------|-------------------|
| Identity | 6 | 2 (LEGACY + QUARANTINED) | Decommission person_identities (empty), quarantine IdentityEngine already done | **NO** — distinct domains |
| Workspace | 3 | 0 | None — legacy stores read-only | **NO** |
| Business Objects | 5 | 1 (founder_objects) | Migrate 5 write sites from founder_objects → sh_objects | **YES** — duplicate writes violate one-canonical-truth |

**Revised P1 blockers for G1.1-FINAL:**
1. ~~Identity convergence~~ — no true active conflict
2. ~~Workspace convergence~~ — no true active conflict
3. **Business object convergence** — founder_objects has 5 active write sites that must migrate to sh_objects (P1)
4. CI independently verified
5. Full regression suite clean